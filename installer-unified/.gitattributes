# Auto-detect text files and normalize line endings
* text=auto

# Shell scripts must use LF and be executable
*.sh text eol=lf

# Batch files use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary

# Lock files - don't diff
package-lock.json -diff
Cargo.lock -diff

